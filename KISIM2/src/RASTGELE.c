#include "stdlib.h"
#include "stdio.h"
#include "RASTGELEKISI.h"
/*
struct Rastgele {
	int rastgele;

	struct long mtemp() {
        long *temp;
        temp = System.nanoTime();
        return temp;
    };
		int RastRandom(int rakam3, int rakam4) {
        while (true) {
            rastgele = (int) ((mtemp()) % 123);
            if (rastgele >= rakam3 && rastgele < rakam4) {
                break;
            }
        }
        return rastgele;

    }
  
};*/